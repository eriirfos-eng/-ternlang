requirements.base.txt

# runtime deps
jsonschema>=4.23.0
requirements.dev.txt

# dev/test/tooling
pytest>=8.2
flake8>=7.1
pytest-cov>=5.0
mypy>=1.11
requirements.txt

# single entry point that includes both
-r requirements.base.txt
-r requirements.dev.txt
